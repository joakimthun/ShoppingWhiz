'use strict';

shoppingWhiz.factory('shoppingListService', function($resource, urlHelper) {
    var apiUrl = 'http://shoppingwhizapi.azurewebsites.net/';
    var db = window.localStorage;
        return {
                
         
        };
});